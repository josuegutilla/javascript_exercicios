var x = 0;

while(x <= 5) {
    console.log('Tentando a repetição' + ' ' + x);

    //incremetador
    x++;
}

var arr = ['Login:', 'Senha:', 'Email:', 'Confimação de email:', 'CPF:'];
var j = 0;

while(j <= 4) {
    console.log(arr[j]);

    j++;
}

var nome = 'josuégutilla';
var i = 0;

while(i <= 11) {
    console.log(nome[i]);

    i+= 1;
}